(function(){ const btn = document.querySelector('#theme-toggle'); function apply(dark){ document.documentElement.classList.toggle('dark-mode', dark); if(btn) btn.textContent = dark ? '🌙' : '🌞'; } const stored = localStorage.getItem('parkEaseDark'); apply(stored === 'true'); if(btn) btn.addEventListener('click', ()=>{ const is = !document.documentElement.classList.contains('dark-mode'); localStorage.setItem('parkEaseDark', is); apply(is); });})();